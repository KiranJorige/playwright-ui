# Development Environment Configuration
# Checked into git - contains non-sensitive information only

# Application
APP_ENV=development
BASE_URL=https://opensource-demo.orangehrmlive.com

# Logging
LOG_LEVEL=debug

# Authentication (local only)
# Set credentials in config/.env.dev.local (gitignored)
# In Ci use secrets to set credentials