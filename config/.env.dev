# Development Environment Configuration
# Checked into git - contains non-sensitive information only

# Application
App_ENV=development
BASE_URL=http://localhost:3000

# Logging
LOG_LEVEL=debug

# Authentication (local only)
# Set credentials in config/.env.local (gitignored)